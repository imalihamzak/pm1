[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Required for Next.js API routes and server-side rendering
[functions]
  node_bundler = "esbuild"

